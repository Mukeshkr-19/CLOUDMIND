# GitHub Manual Steps

Repository settings are external state and are not required for local code correctness.

## About

Set the description exactly to:

> Policy-governed AIOps and closed-loop SRE platform with Prometheus telemetry, dependency-aware RCA, safe auto-remediation, and recovery verification.

Leave Website blank until a real demo or documentation site exists.

Set these topics:

`aiops`, `site-reliability-engineering`, `observability`, `prometheus`, `grafana`, `alertmanager`, `root-cause-analysis`, `auto-remediation`, `incident-response`, `chaos-engineering`, `docker`, `flask`, `python`, `github-actions`, `devsecops`, `gemini-api`

## Social Preview

Repository → Settings → Social preview → Edit → Upload `docs/social-preview.png`.

The asset is 1280×640 PNG and under 1 MB.

## Release

Do not create tags until the historical pre-AIOps commit is confirmed and this hardening branch is merged and green. Proposed tags are `v1.0.0-sre` and `v2.0.0-aiops`. Release creation and branch deletion require explicit user approval.

## Authentic Captures

After a verified live demo, replace or add screenshots for the AIOps Grafana panels, one complete incident decision, and recovery verification. Do not upload generated substitutes.
